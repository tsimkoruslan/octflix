import React, {FC} from 'react';

import css from './switcher.module.css';

const Switcher: FC = () => {
    return (
            <div className={css.Switcher}>
                sw
            </div>
    );
};

export {
    Switcher
};